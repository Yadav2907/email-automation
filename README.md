# email-automation
An n8n workflow that automatically reads incoming Gmail messages, uses Google Gemini to decide whether a reply is warranted (recruiter confirmations, informal messages, direct requests for a response), drafts a context-appropriate reply, and sends it back in the same thread.
